import React from 'react';

const ApiStatus = () => {
    // Temporarily disabled to avoid CORS errors during development
    // App uses mock data when backend is unavailable
    return null;
};

export default ApiStatus;