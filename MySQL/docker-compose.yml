version: '3'
services:
  mysql-db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: treepaech
      MYSQL_DATABASE: Cloud
      MYSQL_USER: test
      MYSQL_PASSWORD: guntreepaech
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql

  init-script:
    image: mysql:latest
    command: [ '--host=mysql-db', '--user=root', '--password=treepaech', '--database=Cloud', '-e', 'source /docker-entrypoint-initdb.d/init.sql' ]
    volumes:
      - ./init-scripts:/docker-entrypoint-initdb.d

volumes:
  mysql_data: